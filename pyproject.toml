[project]
name = "genie"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "einops>=0.8.1",
    "flax>=0.12.2",
    "jax>=0.8.2",
    "numpy>=2.3.5",
    "torch>=2.9.1",
    "ty>=0.0.2",
]

[dependency-groups]
dev = [
    "ty>=0.0.2",
]
